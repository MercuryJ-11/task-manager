<br>
<br>
<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li><%= link_to task.title, task_path(task.id) %>
  <%= link_to 'delete', destroy_path(task.id), method: :delete, data: {confirm: "delete #{task.title}?"} %>
  </li>
  <% end %>
</ul>
<%= link_to 'Add a new task', tasks_new_path %>
